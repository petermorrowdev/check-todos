[project]
name = "check-todos"
version = "0.1.0"
description = "Scan source code for TODO comments checking they have a task ID"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tree-sitter",
    "tree-sitter-html",
    "tree-sitter-python",
    "tree-sitter-typescript",
]
